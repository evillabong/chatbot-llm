# Pendientes (backlog)

Registro de requerimientos detectados pero **no realizados aún**. Se va actualizando: cuando
algo no se pueda/deba hacer en el momento, se anota aquí con contexto suficiente para retomarlo.

Prioridad: 🔴 alta · 🟡 media · 🟢 baja.

| # | Pendiente | Prioridad | Contexto |
|---|---|---|---|
| 1 | **Respuestas tipadas en `Mimo.Admin.Api`** | 🟡 | Igual que el pass hecho en `Mimo.Api` (`.Produces<T>()`), para que el futuro cliente de la Admin API quede tipado. Hacer al construir `Mimo.Admin.App`. |
| 1b | **Respuestas tipadas en endpoints restantes de `Mimo.Api`** | 🟡 | Ya tipados: Auth, Agents, Roles, Documents, Tickets (GET + claim/resolve/close), Conversations (detail). Faltan: InternalChat, Survey, y los POST anónimos de Conversations. |
| 20 | **Fase C: verificación SignalR en navegador** | 🟢 | Fase C **completa** (inbox, triage, mensajería en vivo, transferencias, cola en vivo, chat interno y encuesta). Falta validar la entrega de eventos SignalR en navegador con 2 clientes (transporte y contrato ya verificados por REST/cliente .NET). Para producción ver CORS de SignalR (#5). |
| 21b | **Transferencia: persistir TransferRecord y revisar cardinalidad ticket↔conversación** | 🟡 | El endpoint `/tickets/transfer` crea el ticket destino pero **no persiste el `TransferRecord`** (se pierde el historial). Además, al ser conversación↔ticket 1:1, transferir reemplaza el ticket original (cambia el id); evaluar conservar historial de tickets por conversación. |
| 2 | **Vulnerabilidad NU1903** | 🔴 | `System.Security.Cryptography.Xml 9.0.5` (transitivo, vía Data Protection) con CVE de severidad alta. Fijar/actualizar a versión parcheada. |
| 18 | **UI de hora/días de atención y campos `int?`** | 🟢 | `/configuracion` edita un subconjunto; la hora/días de atención (`Time`/`int[]`) y los `int?` (timeouts) se preservan por round-trip pero no se editan. Los `int?` salen como `UntypedNode` en el cliente (OpenAPI 3.0 + nullable); evaluar emitir tipos limpios o DTO dedicado. |
| 19 | **Cifrar secretos de canal en `TenantConfiguration`** | 🟡 | Tokens de WhatsApp/Telegram/etc. viajan en claro en el JSON de configuración. Aplicar cifrado en reposo análogo al de las API keys de IA (ADR 0010). |
| 4 | **Binario de Tailwind en CI/clones** | 🟡 | `src/Mimo.Ui/tools/tailwindcss.exe` está gitignorado (38 MB). `dotnet publish`/CI/un clon nuevo no tendrán CSS (`mimo.min.css`). Añadir fetch (script o target que lo descargue a `Mimo.Ui/tools/` si falta). |
| 5 | **CORS para SignalR** | 🟡 | La consola de agente usará hubs; los websockets cross-origin requieren orígenes explícitos + `AllowCredentials` (el CORS actual `AllowAnyOrigin` no sirve con credenciales). |
| 6 | **Promover `develop` → `main` + push** | 🟡 | `v0.1.0` y `v0.2.0` están etiquetados en `develop`; `main` quedó atrás. Decidir publicación del release y push al remoto. |
| 7 | **Re-desplegar a IIS** | 🟡 | IIS corre código previo a varios cambios. Correr `scripts/deploy-tenant.ps1` (mimo.api + mimo.app) y `scripts/deploy-admin.ps1` (mimo.admin.api + mimo.admin.app) para actualizar. |
| 8 | **Tests de integración con PostgreSQL real (Testcontainers)** | 🔴 | Cubrir aislamiento multi-tenant/`search_path`, dequeue `Serializable` de la cola, firmas de webhook y la ruta de compensación del provisioning. Bloqueado hoy por falta de Docker. |
| 9 | **Refresh tokens / revocación JWT** | 🟢 | ADR 0002: hoy no hay refresh; expiración → re-login. |
| 10 | **Gestión de SuperAdmins/agentes con rol elevado tras bootstrap** | 🟢 | Sólo existe el bootstrap del primer SuperAdmin (`/auth/setup`); falta crear más de forma autenticada. |
| 11 | **Validar `AllowedProviders` contra conectores existentes** | 🟢 | El upsert de política de plan acepta proveedores sin conector; hoy es laxo a propósito. |
| 12 | **Frontend fases B–E** | 🟡 | B: admin de tenant completo · C: consola de agente (Callbell, SignalR) · D: `Mimo.Admin.App` · E: WebChat embebible. |
| 13 | **Cifrado de API key bajo IIS** | 🟢 | El anillo de llaves de Data Protection compartido se configuró; falta verificar permisos/funcionamiento real bajo IIS tras el primer re-deploy. |
| 22 | **Mejora continua: el asistente aprende con el uso (plus premium)** | 🟢 | Capacidad NO-core, gobernada por plan y **por organización (datos aislados, nunca cruzados)**. Diseño detallado (3 fases, arquitectura, métricas, riesgos) en [docs/vision-mejora-continua.md](../vision-mejora-continua.md). Resumen: (1) crecimiento de conocimiento asistido (RAG + sugerencias con aprobación humana); (2) clasificador de derivación; (3) fine-tuning por tenant opcional. **No reentrenar el LLM** por defecto. **Captura ya lista**; falta instrumentar *retrieval score* + flag `knowledge_gap`, worker batch y UI de curación. |
| 16 | **Paginación server-side en `MimoTable`** | 🟢 | Hoy la lista de funcionarios trae todo. Cuando crezcan los datos, añadir paginación/orden en el endpoint y en el componente. |
| 23 | **Acciones del asistente vía MCP (tool-use)** | 🟡 | El bot consume APIs parametrizadas para responder con datos reales y ejecutar acciones. Diseño: servidor MCP por organización que publica herramientas (esquema + auth + límites); el gateway de IA ofrece solo las autorizadas y media la ejecución (credenciales por tenant cifradas, allowlist, auditoría, lectura vs efectos). Detalle en [docs/anexo-tecnico-integraciones.md](../anexo-tecnico-integraciones.md) §4. |
| 24 | **Automatización: flujos de trabajo, campañas y tareas** | 🟡 | Módulos de roadmap. Flujos "evento→condición→acción" sobre los eventos ya existentes; campañas salientes segmentadas/programadas por canal; tareas como entidad con responsable/vencimiento ligada a conversación/ticket/oportunidad. |
| 25 | **Completar/certificar conectores de canal** | 🟡 | WhatsApp (Cloud API), Telegram (Bot API), Facebook/Instagram (Meta Graph): hoy andamiados (conector + webhook + verificación de firma). Falta completar envío/recepción real y certificación con cada proveedor. |
| 26 | **Ventas: leads + pipeline + sync CRM** | 🟢 | Capacidad opcional (propuesta §4.10): captura de oportunidades desde la conversación, pipeline básico por etapas, tareas de seguimiento y sincronización opcional con CRM externo. |
| 17 | **Endpoint y UI de categorías de documentos** | 🟡 | No hay endpoint CRUD de `DocumentCategory`, así que la página `/conocimiento` no permite asignar categoría (queda null). Exponer `/document-categories` y agregar el desplegable en el formulario. |
| 14 | **Variables de entorno en IIS para Development** | 🟡 | Para que las publicaciones locales reciban el CORS permisivo hay que setear `ASPNETCORE_ENVIRONMENT=Development` en el `web.config` de cada sitio. Como en Development NO cargan user-secrets bajo el App Pool, hay que inyectar también `ConnectionStrings__Default` (`Host=localhost;Port=5432;Database=mimo;Username=postgres;Password=100`) y `Jwt__Key` (misma clave en ambas APIs). Pendiente: hacerlo al re-desplegar (#7). |

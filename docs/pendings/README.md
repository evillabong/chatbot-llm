# Pendientes (backlog)

Registro de requerimientos detectados pero **no realizados aún**. Se va actualizando: cuando
algo no se pueda/deba hacer en el momento, se anota aquí con contexto suficiente para retomarlo.

Prioridad: 🔴 alta · 🟡 media · 🟢 baja.

| # | Pendiente | Prioridad | Contexto |
|---|---|---|---|
| 1 | ✅ **Respuestas tipadas en `Mimo.Admin.Api`** (hecho) | 🟢 | Todos los endpoints del Admin API exponen `.Produces<T>()`/estado; faltaban dos: `POST /auth/setup` (ahora devuelve `SetupSuperAdminResponse`) y `POST /ai/connectors/activate` (ahora **204** en vez de objeto anónimo). SDK Admin regenerado **sin `UntypedNode`/`Stream`**. |
| 31 | ✅ **WebChat embebible — corte 2 (funcionario en vivo)** (hecho) | 🟢 | El widget conecta `ChatHub` (SignalR) y recibe en vivo mensajes de funcionario/bot (`MessageReceived`), `AgentJoined` y `StatusChanged`; al cerrar muestra la **encuesta** embebida. Respaldo REST si SignalR no conecta. [ADR 0019](../adr/0019-acceso-cross-origin-anonimo-al-chathub.md). **Verificado E2E** (cliente SignalR real): conexión WS, tenant por `?tenant_slug`, join, `SendMessage` → eco + respuesta. Pendiente menor: **posición de cola** por usuario (hoy no hay evento por-ciudadano) y E2E de dos partes (funcionario+ciudadano). |
| 32 | ✅ **Rate limiting / anti-abuso en la superficie del WebChat** (hecho) | 🟢 | Rate limiter integrado de .NET por IP: crear conversación / encuesta / handshake del hub a 10/min (`webchat-start`) y mensajes a 30/min (`webchat-chat`); 429 con `Retry-After`. **Verificado:** 11º POST `/conversations` → 429 (`Retry-After: 60`), `/webchat/config` sin límite. Pendiente: captcha opcional y throttling de `SendMessage` **dentro del hub** (la ráfaga sobre un WS ya abierto no pasa por el limitador HTTP). Ver #34. |
| 34 | ✅ **IP real tras proxy + throttling en el hub** (hecho) | 🟢 | (a) `UseForwardedHeaders` (X-Forwarded-For/Proto) con proxies de confianza configurables (`ForwardedHeaders:KnownProxies`; por defecto solo loopback). **Verificado:** el rate limiter particiona por la IP reenviada (buckets distintos por `X-Forwarded-For`). (b) Throttling por conexión en `ChatHub.SendMessage`: 30 mensajes/min (alineado con el límite REST), responde evento `Error` al exceder. **Verificado:** ráfaga de 35 → 30 procesados + 5 rechazados. Limpieza en `OnDisconnectedAsync`. |
| 33 | ✅ **Resiliencia del orquestador si el LLM no está disponible** (hecho) | 🟢 | La búsqueda semántica (embeddings) ya no propagaba: si el LLM falla se degrada a SIN contexto y el intento de chat usa su fallback con mensaje amable. **Verificado:** sin credenciales de LLM el endpoint de mensajes responde **200** con texto de respaldo en vez de 500. Mejora futura opcional: escalar a funcionario ante caída total del LLM (hoy responde "intenta nuevamente"). |
| 30 | ✅ **Lock/leader-election antes de escalar `Mimo.Worker`** (hecho) | 🟢 | Claim atómico de entregas con `FOR UPDATE SKIP LOCKED` + marca `claimed_by`/`claimed_at` y estado `InProgress`; recupera claims vencidos (>5 min). [ADR 0020](../adr/0020-claim-atomico-de-entregas-de-webhook.md). **Verificado E2E con 2 instancias** del worker: 6 entregas → 6 recibidas, sin duplicados. `InactivityTimeoutWorker` es idempotente (`ExecuteUpdate`), no requiere claim. |
| 1b | ✅ **Respuestas tipadas en endpoints restantes de `Mimo.Api`** (hecho) | 🟢 | Tipados Survey (`POST`), Conversations (`POST /` y `/messages`) y, de paso, Documents `/reindex` y Tickets `/notes` (ambos ahora **204**). SDK regenerado sin `UntypedNode`/`Stream` salvo: los modelos de config con `int?` (limitación OpenAPI 3.0 → #18) y el webhook **entrante** de canales (sin cuerpo, no lo consume el SDK). InternalChat ya estaba tipado. |
| 20 | **Fase C: verificación SignalR en navegador** | 🟢 | Fase C **completa** (inbox, triage, mensajería en vivo, transferencias, cola en vivo, chat interno y encuesta). Falta validar la entrega de eventos SignalR en navegador con 2 clientes (transporte y contrato ya verificados por REST/cliente .NET). Para producción ver CORS de SignalR (#5). |
| 21b | ✅ **Transferencia: persistir TransferRecord** (hecho) | 🟢 | `/tickets/transfer` ahora persiste el `TransferRecord`, **anclado a la `ConversationId`** (estable) y no al ticket: al transferir, el ticket de origen se reemplaza (1:1) y se eliminaba, y un FK con cascada borraba el historial. Reanclado a la conversación (FK cascade), `TicketId` queda informativo. **Verificado:** tras transferir, el registro persiste aunque el ticket de origen se borre. Migración `TransferRecordByConversation`. Queda abierto (mayor) el rediseño 1:1→1:N de ticket↔conversación si se quisiera conservar varios tickets por conversación. |
| 2 | ✅ **Vulnerabilidad NU1903** (hecho) | 🟢 | `System.Security.Cryptography.Xml 9.0.5` (transitivo vía `Microsoft.AspNetCore.DataProtection.Extensions`, en `Mimo.Worker` y `Mimo.UnitTests`) con CVE alto. Se subieron las referencias de Data Protection a **10.0.9** (10.0.0 GA tenía además un CVE crítico propio). **Verificado:** `dotnet list package --vulnerable --include-transitive` sin paquetes vulnerables en los 12 proyectos; build con 0 avisos. |
| 18 | 🟡 **Campos `int?` de configuración como `UntypedNode`** (parcial) | 🟢 | (a) ✅ **Hecho**: `/configuracion` edita hora inicio/fin (`Time`) y días (`int[]`); round-trip verificado. (b) **Pendiente**: los `int?` (timeouts) salen como `UntypedNode` (OpenAPI 3.0 + nullable value types). **Evaluado y descartado emitir OpenAPI 3.1**: con Kiota 1.32 **empeora** (≈20 modelos pasan a `UntypedNode`, los `int?` siguen sin tiparse) — confirma la elección de 3.0 de [ADR 0011](../adr/0011-arquitectura-frontend-blazor-wasm-aislado.md). Camino viable: **DTO de configuración dedicado** que exponga los timeouts como `int` no-nullable con centinela (p. ej. 0 = sin límite) y mapee a/desde `int?`. Hasta entonces se preservan por round-trip. |
| 19 | ✅ **Cifrar secretos de canal en `TenantConfiguration`** (hecho) | 🟢 | El único secreto del modelo, `TelegramBotToken`, se **cifra en reposo** (`ISecretProtector`, ADR 0010) al guardar y es **write-only**: la lectura (`GET /tenant/configuration`) lo enmascara y un guardado en blanco **conserva** el valor existente. **Verificado:** en BD queda como blob de Data Protection (no texto plano), no se filtra en GET/PUT, y un guardado en blanco no lo borra. Futuros tokens de canal deben seguir el mismo patrón. |
| 4 | ✅ **Binario de Tailwind en CI/clones** (hecho) | 🟢 | Target MSBuild `EnsureTailwindCli` en `Mimo.Ui.csproj`: si falta el CLI, descarga la versión fijada (v3.4.17) del release de Tailwind antes de compilar el CSS; selecciona el binario por SO/arquitectura (Windows/Linux/macOS, x64/arm64) y hace `chmod +x` en Unix. **Verificado:** moviendo el `.exe` y borrando `mimo.min.css`, el build lo descarga y regenera el CSS. El binario sigue gitignored. |
| 5 | ✅ **CORS para SignalR** (config lista) | 🟢 | La política CORS de producción ahora usa **orígenes explícitos + `AllowCredentials`** (`Cors:AllowedOrigins` en config; placeholder en `appsettings.json`), habilitando los hubs autenticados cross-origin (TicketHub) sin `AllowAnyOrigin`. La consola conecta con JWT por `access_token` (sin cookies). Development sigue permisivo. **Falta solo la verificación real** en un despliegue de dos orígenes (mimo.app ≠ mimo.api), ligada a #7 (re-deploy) y #20 (prueba en navegador). |
| 6 | 🟡 **Promover a `main` + push** (develop publicado) | 🟢 | ✅ El trabajo de la sesión (rama `feature/feat-claude-frontend-cimientos`) se **mergeó por fast-forward a `develop`** y se **pusheó a `origin`** junto con todos los tags (`v0.1.0`…`v0.7.2`). **Pendiente (decisión del usuario):** promover `develop` → `main` vía PR (flujo de PR del repo) y publicar el release en `main`. |
| 7 | **Re-desplegar a IIS** | 🟡 | IIS corre código previo a varios cambios. Correr `scripts/deploy-tenant.ps1` (mimo.api + mimo.app) y `scripts/deploy-admin.ps1` (mimo.admin.api + mimo.admin.app) para actualizar. |
| 8 | **Tests de integración con PostgreSQL real (Testcontainers)** | 🔴 | Cubrir aislamiento multi-tenant/`search_path`, dequeue `Serializable` de la cola, firmas de webhook y la ruta de compensación del provisioning. Bloqueado hoy por falta de Docker. |
| 9 | 🟡 **Refresh tokens / revocación JWT** (backend `Mimo.Api` hecho) | 🟢 | ✅ Funcionarios: login emite refresh token (hash en BD, esquema tenant), `POST /auth/refresh` (rota) y `POST /auth/logout` (revoca); vida configurable. [ADR 0022](../adr/0022-refresh-tokens-de-funcionarios.md). **Verificado E2E** (rotación + revocación). **Pendiente:** equivalente para SuperAdmin en `Mimo.Admin.Api`, **cableado en el front** (guardar refresh + renovar al expirar) y limpieza periódica de tokens vencidos. |
| 10 | ✅ **Gestión de SuperAdmins tras bootstrap** (hecho) | 🟢 | `Mimo.Admin.Api`: `/superadmins` (política SuperAdmin) — listar, **crear** (email único → 409) y **desactivar** (con guardas: no a sí mismo, no al último activo). El bootstrap del primero sigue en `/auth/setup`. **Verificado E2E** (crear/duplicado/desactivar/auto-desactivar/401 sin token). |
| 11 | ✅ **Validar `AllowedProviders` contra conectores existentes** (hecho) | 🟢 | El upsert de política de plan (`PUT /ai/plan-policies`) ahora rechaza (400) proveedores que no tengan un conector de IA configurado, listando los desconocidos. **Verificado E2E** (`openai` sin conector → 400; `deepseek` con conector → 200). |
| 12 | ✅ **Frontend fases B–E** (hecho) | 🟢 | B ✅ (conocimiento + configuración) · C ✅ (consola) · D ✅ (Mimo.Admin.App: tenants, planes, conectores, políticas, uso) · E ✅ (WebChat embebible, cortes 1–2). Verificación visual en navegador de las WASM sigue pendiente (QA manual, ver #20/#28). |
| 28 | **Mimo.Admin.App** | 🟢 | **Fase D completa**: tenants, planes, uso, conectores de IA (API key write-only) y políticas de IA por plan. Verificación visual en navegador pendiente (como el resto de WASM). |
| 13 | **Cifrado de API key bajo IIS** | 🟢 | El anillo de llaves de Data Protection compartido se configuró; falta verificar permisos/funcionamiento real bajo IIS tras el primer re-deploy. |
| 22 | 🟡 **Mejora continua: el asistente aprende con el uso (plus premium)** (cortes 1–2 hechos) | 🟢 | Capacidad NO-core, gobernada por plan y **por organización (datos aislados, nunca cruzados)**. Diseño en [docs/vision-mejora-continua.md](../vision-mejora-continua.md): (1) crecimiento de conocimiento asistido; (2) clasificador de derivación; (3) fine-tuning por tenant opcional. **No reentrenar el LLM** por defecto. ✅ **Corte 1 ([ADR 0023](../adr/0023-instrumentacion-de-vacios-de-conocimiento.md)):** *retrieval score* + flag `knowledge_gap` — `SearchScoredAsync`, el orquestador registra `KnowledgeQuerySignal` por consulta (best-effort, sin señal falsa si el LLM está caído), `GET /knowledge/gaps`. ✅ **Corte 2:** **bandeja de curación HITL** — entidad `KnowledgeSuggestion` + `/knowledge/suggestions` (listar/crear/aprobar/descartar, TenantAdmin); aprobar **publica un `Document`** (degrada el embedding sin LLM); UI `/mejora-continua` (vacíos + sugerencias, crear borrador desde un vacío). **Verificado E2E** (ciclo crear→aprobar→documento publicado→descartar→409 al re-revisar; filtros por estado; authz) + unit tests del umbral. **Pendiente (corte 3 + Fases 2–3):** worker batch que **genera** borradores con IA (agrupa vacíos + resumen vía gateway; requiere LLM); clasificador de derivación; fine-tuning opcional. |
| 16 | ✅ **Paginación server-side** (funcionarios) | 🟢 | Endpoint `GET /agents/paged` (`PagedResult<AgentResponse>`, page/pageSize/isActive) reusando `ToPagedResultAsync`; componente reutilizable `MimoPager` y la página `/funcionarios` paginan (20/pág). `GET /agents` plano se conserva para selectores (chat interno). **Verificado E2E** (page1/última, totales, hasNext/Prev). Aplicar el mismo patrón a otras tablas cuando haga falta. |
| 23 | 🟡 **IA extensible: herramientas/MCP, servidores MCP externos y agentes por rol** (corte 1 hecho) | 🟡 | (a) Tool-use vía MCP por organización (esquema+auth+límites; el gateway media y audita). (b) ✅ **Registrar servidores MCP externos — corte 1 ([ADR 0024](../adr/0024-catalogo-de-servidores-mcp-externos.md)):** catálogo `McpServer` + endpoints `/mcp-servers` (TenantAdmin) con token cifrado write-only (ISecretProtector), allowlist deny-by-default, validación de endpoint (400/409) y UI `/mcp-servers`. **Verificado E2E** (CRUD; token no expuesto y cifrado en reposo; blanco conserva, rotación reemplaza; allowlist normalizada; timeout acotado; authz; 204→404). **Pendiente:** la **invocación** mediada por el gateway (anti-SSRF como `ChatbotApiCaller`), scopes por herramienta, auditoría/cuotas. (c) **Agentes de IA por rol/tipo de atención**: perfiles con instrucciones, subconjunto de conocimiento, herramientas/MCP y tono; el gateway elige el agente por contexto. Todo por tenant y aislado. Detalle en [docs/anexo-tecnico-integraciones.md](../anexo-tecnico-integraciones.md) §4 y §4.bis. |
| 29 | **Módulo de Interoperabilidad (API de integración + API keys + webhooks salientes)** | 🟡 | Superficie de integración por organización (gestión en `Mimo.App`, TenantAdmin). (a) ✅ **API de integración** (corte 2, hecho): superficie pública `/integration/v1/*` autenticada por **API key** (esquema propio `X-Api-Key`, no JWT); el tenant se resuelve por la key ([ADR 0015](../adr/0015-autenticacion-por-api-key-y-api-de-integracion.md)); endpoints `GET /me`, `POST /conversations`, `GET /conversations/detail`. Falta: ampliar subconjunto (tickets, conocimiento…), **scopes por clave** y OpenAPI dedicado. (b) ✅ **API keys** (corte 1, hecho): generar (mostrar una vez), guardar solo **hash** SHA-256 + prefijo visible, último uso, revocación; aislado por tenant (`public.api_keys` con `TenantId`); endpoints `/integration/api-keys` (TenantAdmin) y página `/api-keys`. Falta: scopes y rotación. (c) ✅ **Webhooks salientes** (corte 3, hecho): suscripción a eventos (`conversation.created`, `ticket.assigned/resolved`, `survey.recorded`), entrega firmada (HMAC-SHA256 por suscripción, secreto cifrado mostrado una vez) con reintentos/backoff por worker y bitácora; gestión en `/integration/webhooks` (TenantAdmin); [ADR 0016](../adr/0016-webhooks-salientes-con-firma-hmac-y-reintentos.md). Falta: backoff/eventos configurables por suscripción y reintento manual. **Módulo de Interoperabilidad completo** salvo refinamientos (scopes por clave, OpenAPI dedicado, lo anterior). Detalle en [docs/anexo-tecnico-integraciones.md](../anexo-tecnico-integraciones.md) §6.bis. |
| 27 | 🟡 **Chatbot por catálogo de opciones (flujos guiados, sin IA)** (cortes 1–3 hechos) | 🟢 | ✅ C1: motor determinista (Message/Menu/Escalate), `FlowNodeId`, integración en orquestador, endpoints `/chatbot/flows`. ✅ C2: nodo **`Input`** (captura+validación anti-ReDoS), **variables** (`FlowState`) y sustitución `{var}`. ✅ C3: nodo **`ApiCall`** (HTTP con `{var}`, captura de respuesta, bifurca éxito/fallo) ejecutado por el orquestador (motor sigue puro) con **anti-SSRF** (allow-list deny-by-default, bloqueo de IPs internas/metadata, sin redirecciones, timeout, tamaño máx.). **Verificado E2E** (llamada permitida con captura + host bloqueado) y unit tests. [ADR 0021](../adr/0021-chatbot-por-opciones-flujos-guiados.md). **Pendiente:** validación completa de transiciones, mitigar **DNS rebinding** y **constructor visual** de flujos. Propuesta §4.16. |
| 24 | **Automatización: flujos de trabajo, campañas y tareas** | 🟡 | Módulos de roadmap. Flujos "evento→condición→acción" sobre los eventos ya existentes; campañas salientes segmentadas/programadas por canal; tareas como entidad con responsable/vencimiento ligada a conversación/ticket/oportunidad. |
| 25 | **Completar/certificar conectores de canal** | 🟡 | WhatsApp (Cloud API), Telegram (Bot API), Facebook/Instagram (Meta Graph): hoy andamiados (conector + webhook + verificación de firma). Falta completar envío/recepción real y certificación con cada proveedor. |
| 26 | **Ventas: leads + pipeline + sync CRM** | 🟢 | Capacidad opcional (propuesta §4.10): captura de oportunidades desde la conversación, pipeline básico por etapas, tareas de seguimiento y sincronización opcional con CRM externo. |
| 17 | ✅ **Endpoint y UI de categorías de documentos** (hecho) | 🟡 | `DocumentCategoryEndpoints` expone `/document-categories` (GET lectura para Agent; POST/DELETE para TenantAdmin) sobre el esquema del tenant: alta valida nombre duplicado y padre inexistente (409), borrado bloquea si hay documentos o subcategorías que la referencian (409). `DocumentCategoriesService` (cliente Kiota) + `/conocimiento` ahora trae el desplegable de categoría en el formulario y un modal «Nueva categoría». **Verificado E2E:** crear → listar → duplicado 409 → documento con `categoryId` → borrar categoría en uso 409 → desligar/borrar → 204. |
| 14 | **Variables de entorno en IIS para Development** | 🟡 | Para que las publicaciones locales reciban el CORS permisivo hay que setear `ASPNETCORE_ENVIRONMENT=Development` en el `web.config` de cada sitio. Como en Development NO cargan user-secrets bajo el App Pool, hay que inyectar también `ConnectionStrings__Default` (`Host=localhost;Port=5432;Database=mimo;Username=postgres;Password=100`) y `Jwt__Key` (misma clave en ambas APIs). Pendiente: hacerlo al re-desplegar (#7). |

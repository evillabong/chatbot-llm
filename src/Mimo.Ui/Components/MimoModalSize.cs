namespace Mimo.Ui.Components;

/// <summary>Tamaños de <see cref="MimoModal"/> (no expone el enum de Flowbite a las apps).</summary>
public enum MimoModalSize
{
    Small,
    Medium,
    Large,
    ExtraLarge
}
